# AgentMesh v0.5 设计：需求对账单架构（Reconciliation Ledger）

> 状态：设计修订 v2（2026-09-08；原稿 2026-09-07 定稿，v2 吸收首轮评审意见）
> 上游输入：newsradar 实测（组长消耗 3-4 倍于 worker）、P-079/P-080 立案、前人方案调研（Kiro Spec / Anthropic 多智能体系统 / ClaudeCode / codex 源码）
> v2 修订要点：①§6 归档重写——引擎不可改写宿主侧组长历史，主机制改为"返回时截断"，清场仅作用于 AgentMesh 侧会话；②目标口径分层（"100%" 降为"100% 可对账"，15%/5-12min 按 Batch 分层并挂前置条件）；③leaderShare 度量提前至 Batch 1；④补终态 NEEDS_RULING、越界升道、副官探针、抽检人工回流四条规则

---

## 1. 背景与三目标

| #   | 目标                         | 基线（newsradar）                                   | 目标                                                                                    |
| --- | ---------------------------- | --------------------------------------------------- | --------------------------------------------------------------------------------------- |
| 1   | 主模型 token 消耗变少        | 组长占比 ≈75-80%（3-4 倍于 worker，O(n²) 会话累积） | Batch 1 后 ≤40-50%；Batch 2 后 ≤15%                                                     |
| 2   | 任务精度可证（符合需求文档） | "感觉都做了"，覆盖度不可证                          | 逐条对账单，100% **可对账**（不丢账硬保证；语义正确性由验收/评审/抽检层单独度量）       |
| 3   | 简单任务速度快               | 全流程税 20-25min（多 stage + 评审 + 轮询）         | 快车道**中位数** 5-12min（前置：延迟路由 + P-079 重试；否则只有中位数可承诺，尾部无界） |

三目标住在架构三层，互不冲突：token 在**返回形态层**（P-080）、精度在**数据结构层**（需求 ID 贯穿）、速度在**调度策略层**（分诊+路由）。公共依赖只有一个：**需求 ID 贯穿（threading）**。

## 2. 决策锁定（2026-09-07 用户拍板；2026-09-08 v2 追加后四行）

| 分歧                 | 决策                                                                                                                     |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 蒸馏通道             | 副官初稿 + 组长裁决标记项（decidable:false 才找组长）                                                                    |
| 分诊机制             | 引擎确定性规则 + 边缘裁决（gated 闸道，run 前同步裁决）                                                                  |
| 首发主线             | 对账单先行，P-080①②（compact 默认+指针化）同批搭载                                                                       |
| 抽检策略             | 快车道固定比例 10-20% 强制补审（seeded 随机，可审计）                                                                    |
| 组长上下文清场（v2） | 主机制 = 返回时截断（Tier 0，P-080②）；"事后清场"仅作用于 AgentMesh 侧会话——宿主侧组长历史引擎不可改写，这是 v0.5 硬边界 |
| 终态语义（v2）       | ledger 存在 PENDING_RULING 行 → workflow 终态为 NEEDS_RULING（非 DONE），组长显式裁决后引擎重出 ledger 闭环              |
| 分诊道命名（v2）     | 原 "ruling" 道更名 "gated"（闸道），与条目状态 PENDING_RULING 严格区分，避免一词两用                                     |
| 度量先行（v2）       | leaderShare 指标 + lane 聚合提前至 Batch 1 #0——没有尺子就无法宣布后续批次有效                                            |

## 3. 核心数据流

```text
需求文档
   │
   ▼ ① 副官蒸馏（免费模型，输出 schema 强制）
R1..Rn 结构化条目（EARS 语法 + 原文引用 + decidable 标记）
   │   ├─ 引用机械核验：quote 必须是需求文档真实子串（零 token 防编造）
   │   └─ 对数粗核验：条数 vs 文档章节/列表数
   ▼ ② 组长裁决（唯一介入点 1：只处理 decidable:false / PENDING_RULING）
   │
   ▼ ③ spec 生成（R 标签贯穿）
stage.requirements=[R1,R2]；acceptance.commands[].covers=[R1]
   │
   ▼ ④ 引擎分诊（静态确定性判据，run 前判定）
快车道 / 标准 / 完整 / gated 闸道（边缘项 run 前同步交组长裁决，定道后继续）
   │
   ▼ ⑤ 状态机执行（零组长 token）
快车道：worker → exit code →（10-20% 抽检标记 sampled）
        越界写声明文件集之外 → tree guard 检出 → 就地升道 standard 补评审（零组长介入）
标准：worker → 验收 → reviewer → rework 环（≤3）
完整：全流程 + findings 按 R 打标
   │
   ▼ ⑥ 引擎 join → 终态对账单（PENDING_RULING 行>0 → NEEDS_RULING）
        + AgentMesh 侧会话规则化清场（零 token 归档；组长侧由 Tier 0 事前截断保障）
   │
   ▼ ⑦ 组长收对账单（唯一介入点 2：compact + flag 位；返回时已截断，大输出从未进入组长上下文）
```

## 4. 关键数据结构

### 4.1 合同条目：EARS 语法（吸收自 Kiro，五模式）

| 模式     | 句式                               | 示例                                                                                 |
| -------- | ---------------------------------- | ------------------------------------------------------------------------------------ |
| 事件驱动 | WHEN [事件] THEN 系统 SHALL [响应] | WHEN 借阅数>库存 THEN 系统 SHALL 返回 400 {code:'INSUFFICIENT_STOCK'} AND 不修改库存 |
| 异常行为 | IF [条件] THEN 系统 SHALL [响应]   | IF ISBN 非法 THEN 系统 SHALL 返回 422 且不落库                                       |
| 状态持续 | WHILE [状态] 系统 SHALL [行为]     | WHILE 会话激活 系统 SHALL 自动续期 token                                             |
| 可选特性 | WHERE [特性启用] 系统 SHALL [行为] | WHERE MFA 开启 系统 SHALL 登录校验 TOTP                                              |
| 常态     | 系统 SHALL [行为]                  | 系统 SHALL 密码 bcrypt(cost≥12) 存储                                                 |

EARS 的价值：同一语法贯穿蒸馏产出、worker 申报、验收断言三处，且句式可机械映射为测试用例。

### 4.2 requirements.json（副官蒸馏产物 / 初期组长手写）

```json
{
  "source": "需求文档 v3.md",
  "items": [
    {
      "id": "R3",
      "ears": "WHEN 借阅数 > 当前库存 THEN 系统 SHALL 返回 400 {code:'INSUFFICIENT_STOCK'} AND 不修改库存",
      "kind": "event-driven",
      "quote": "库存不足时不能借出（§2.1）",
      "decidable": true
    },
    {
      "id": "R9",
      "ears": null,
      "kind": null,
      "quote": "列表页要美观大方（§5.2）",
      "decidable": false
    }
  ]
}
```

约束：`quote` 必须为源文档真实子串（引擎 substring 核验——**只证明出处，不证明 EARS 改写忠实**；忠实度由组长抽查 + 抽检逃逸率单独度量）；`decidable:false` 条目进人审区，组长裁决改写或显式移入 PENDING_RULING。

### 4.3 WorkflowSpec 扩展

```json
{
  "stages": [
    {
      "name": "implement",
      "roles": ["worker"],
      "requirements": ["R1", "R2", "R3"],
      "dispatch": { "agent": "opencode", "taskTemplate": "…" },
      "acceptance": {
        "commands": [
          { "cmd": "node --test src/borrow.test.mjs", "covers": ["R1", "R3"] },
          { "cmd": "node --test src/isbn.test.mjs", "covers": ["R2"] }
        ],
        "files": ["src/borrow.mjs"]
      }
    }
  ],
  "policy": { "maxReworkRounds": 3, "escalateOn": "any", "lane": "auto" }
}
```

`acceptance.files` 同时作为**声明文件集**：分诊用其判规模（§5），执行期 tree guard 用其检出越界写入——越界即升道补评审，不回滚不重派。

### 4.4 终态对账单 ledger schema（引擎 join 产物，落盘 out/）

```json
{
  "type": "object",
  "required": ["workflowId", "rows", "invariant"],
  "properties": {
    "workflowId": { "type": "string" },
    "rows": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "ears", "status"],
        "properties": {
          "id": { "type": "string" },
          "ears": { "type": "string" },
          "quote": { "type": "string" },
          "status": { "enum": ["PASS", "FAIL", "ESCALATED", "PENDING_RULING"] },
          "evidence": {
            "type": "object",
            "properties": {
              "commands": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "cmd": { "type": "string" },
                    "exitCode": { "type": "integer" },
                    "covers": { "type": "array", "items": { "type": "string" } }
                  }
                }
              },
              "contractMap": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "file": { "type": "string" },
                    "line": { "type": "integer" },
                    "verified": { "type": "boolean" }
                  }
                }
              },
              "findings": { "type": "array", "items": { "type": "string" } }
            }
          }
        }
      }
    },
    "invariant": {
      "type": "object",
      "required": ["requirements", "rows", "ok"],
      "properties": {
        "requirements": { "type": "integer" },
        "rows": { "type": "integer" },
        "ok": { "type": "boolean" }
      }
    }
  }
}
```

**对数硬不变量**（fail-closed）：`n(requirements) == n(PASS) + n(FAIL) + n(ESCALATED) + n(PENDING_RULING)`，不等即强制告警，静默丢失结构性禁止。

**终态语义**（fail-closed）：ledger 存在 `PENDING_RULING` 行 → workflow 终态为 `NEEDS_RULING` 而非 `DONE`——快车道不得携带未裁决条目"安静地完成"；组长裁决后引擎重出 ledger 才闭环。

**跨 stage 需求归属**：同一 R 被多个 stage 覆盖时，其终态 = 各 stage 该 R 证据的**合取**（任一 stage 未通过 → 该 R 不为 PASS）；覆盖该 R 的任一 stage 返工后，其余 stage 对该 R 的旧证据标 stale，以最后一次通过为准。

### 4.5 compact 返回契约（组长唯一视角）

```json
{
  "workflowId": "wf_xxx",
  "status": "done",
  "lane": "fast",
  "sampled": false,
  "flags": {
    "unresolvedP0P1": 0,
    "coverage": "8/9（1 PENDING_RULING）",
    "anomalies": ["circuit_open:opencode/glm-5.3-flash(274s 已恢复)"]
  },
  "ledgerRef": "out/ledger_wf_xxx.json"
}
```

规则：flag 位必带（unresolvedP0P1 计数 / 覆盖率 / 异常事件计数 / 升道事件）；返回体 >2KB 自动落盘 out/ 只回尾部 1.5KB + 路径（P-080②，即 §6 Tier 0）；`status` 可取 `needs_ruling`（存在 PENDING_RULING 行，组长必须裁决闭环后引擎才重出 DONE ledger）。

## 5. 分诊引擎（run 前静态判定 + 运行中升道）

```text
triage(spec, requirements):
  IF stage 预估文件集数 ≤ 2
     AND 每条 R 至少被一条 acceptance command covers
     AND 无跨 stage deps
     AND 无跨 stage contextPolicy 需求
  → lane = "fast"        # 验收即评审 + 10-20% 抽检

  ELSE IF 文件集 3-5 且无风险标记
  → lane = "standard"    # worker + reviewer + rework 环

  ELSE IF 文件集 3-5 且存在风险标记（合同风险/并发/安全词）
  → lane = "gated"      # 闸道：run 前交组长裁决，裁决后转 standard 或 full

  ELSE → lane = "full"   # 全流程 + findings 按 R 打标
```

抽检：快车道终态后 seeded 随机抽 10-20% 强制补 LLM 评审（免费 reviewer），抽检发现的缺陷**人工**回流修订分诊判据——回流是人工调参动作而非自动闭环，修订记录落 PROBLEMS.md。

升道（run 中）：快车道 worker 越界写声明文件集（`acceptance.files`）之外的文件，tree guard 检出 → 引擎就地升道为 standard，追加 reviewer + rework 环，零组长介入；升道事件记入 ledger `anomalies`。

## 6. 上下文归档（v2 重写：事前防注入为主，清场只作用于引擎侧会话）

**架构事实（v2 修正）**：组长会话在宿主侧（host LLM），MCP server 无法改写已投递进宿主历史的 tool result——ClaudeCode microcompact 可行是因为它自己就是宿主，AgentMesh 不是。因此"事后清空组长历史"在 v0.5 架构下**没有执行对象**：对组长的唯一硬机制是**返回时截断（事前防注入）**；"清场"只作用于引擎自己拥有的 AgentMesh 会话。

| 层                                       | 触发                                                                          | 动作                                                                                                                                                                                      | 成本             |
| ---------------------------------------- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| Tier 0 返回时截断（组长侧主机制）        | 每次 MCP 返回时（引擎行为）                                                   | 返回体 >2KB 自动落盘 out/ 只回尾部 1.5KB + 路径 + flag 位（即 P-080②）——大输出**从未进入**组长上下文                                                                                      | 零 token，确定性 |
| Tier 1 规则化清场（仅 AgentMesh 侧会话） | workflow 终态时（引擎行为）                                                   | 该 workflow 的 worker/reviewer/tester 会话原始大输出替换为占位符 `[archived → out/ledger_wf_xxx.json]`，保留对账单 + 最近 1 个 stage 的关键消息——下游 contextSessionIds 复用只带摘要+指针 | 零 token，确定性 |
| Tier 2 LLM 压缩（兜底）                  | AgentMesh 会话 token 估算超阈值（默认 70%，`AGENTMESH_AUTOCOMPACT_PCT` 可配） | 触发 compact_context，LLM 摘要兜底——作用域是 AgentMesh 会话，**救不了宿主侧组长**                                                                                                         | LLM 一次调用     |

组长侧残余与边界：Tier 0 保证新输出不膨胀，但组长会话里**已存在**的历史只能靠宿主自身手段（新开会话 + contextSessionIds 续接，或组长不复述已归档内容的使用纪律）。引擎级事后清场组长历史的前提是把组长内化为引擎管理的 agent——重大架构新增，记为 **vNext 非目标**，v0.5 不做。

借鉴保留：ClaudeCode microcompact 的"规则清场先于 LLM 压缩"思想保留，但边界从时间（60min gap / keepRecent=5）换成 workflow 终态（更贴合编排场景），keepRecent 取"最近 1 个 stage"。

## 7. Schema 强制策略（吸收自 codex final_output_json_schema）

副官蒸馏输出与 worker contract map 申报的结构化输出，按 vendor 能力分两路：

| 通道              | 机制                                                                                                                                           | 强度     |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| codex             | `final_output_json_schema` → Responses API `json_schema`（模型 API 层强制，见 codex-rs/codex-api/src/common.rs create_text_param_for_request） | 原生强制 |
| opencode / claude | 输出后 Zod 校验 → 失败带错误信息重试 1 次 → 再失败 fail-closed（不猜、不静默降级）                                                             | 事后校验 |

ledger 本身由引擎 join 生成（纯内部数据），不依赖 LLM 输出，无校验风险。

副官档位纪律：副官跑在免费档（muse-spark 秒败 / nemotron 僵死前科在案），派发前必须探针确认可用；蒸馏失败走 fail-closed 回退组长手写，不做第二次重试。

## 8. 前人验证矩阵（调研结论浓缩）

| 我们的设计              | 前人对应                                                                    | 来源               |
| ----------------------- | --------------------------------------------------------------------------- | ------------------ |
| 对账单 + R 贯穿         | Kiro 三件套（requirements/design/tasks），task 引用 `_Requirements: [R-x]_` | Kiro 官方文档      |
| 快车道                  | Kiro Quick Spec（well-understood 免审批门）                                 | 同上               |
| 蒸馏查矛盾              | Kiro Analyze Requirements                                                   | 同上               |
| 指针化落盘              | Anthropic "persist to filesystem, pass lightweight references"              | Anthropic 工程博客 |
| 副官免费模型            | Anthropic tier split（lead 前沿 / subordinate 轻量，5-10× 节省）            | 同上               |
| compact 契约+硬上限     | Anthropic "output schema first, hard character ceiling"                     | 同上               |
| 返回时截断 + 引擎侧清场 | ClaudeCode microcompact（宿主自清历史不可移植，仅保留"规则先于 LLM"思想）   | ClaudeCode 源码    |
| schema 强制             | codex final_output_json_schema（API 层）                                    | codex 源码         |
| P-079 重试分类          | CodexErr::is_retryable（Stream/Timeout→true；Fatal/Sandbox→false）          | codex 源码         |

**我们独有（未见于前人）**：①对数硬不变量（Kiro 是 LLM 判断，我们是确定性计数）；②引用机械核验（substring 防蒸馏编造）；③leaderShare 告警 + 健康度熔断自愈。

**成本现实定心丸**（Anthropic 官方数据）：多智能体 ≈ chat 的 15 倍 token 属正常；"token 用量解释 80% 性能方差"；LangChain 实测路由式子代理 5 次调用 9K tokens vs 单体 3 次调用 15K——**调用次数不是成本，上下文复费才是**。

## 9. 落地批次（按依赖排序，非时间排期）

**Batch 1 —— 度量先行 + 对账单核心 + 节流搭载**

> 过渡态写明：Batch 1 落地时蒸馏副官尚不存在（在 Batch 2），requirements.json 由组长手写——组长仍需一次性读需求文档并结构化条目。这已把"每轮重读文档"变成"一次性结构化"，是净赚；但组长占比在本批次只承诺 ≤40-50%，≤15% 是 Batch 2 后目标。

| #   | 改动                                                                                                | 落点              |
| --- | --------------------------------------------------------------------------------------------------- | ----------------- |
| 0   | leaderShare 派生指标 + stats 按 lane 聚合 + >25% 告警（**度量先行**：没有尺子无法宣布后续批次有效） | stats 基建        |
| 1   | WorkflowSpec schema：`requirements`、`commands[].covers`、`acceptance.files` 声明文件集（Zod）      | spec 校验         |
| 2   | requirements.json 格式（EARS+quote+decidable）+ 引用 substring 机械核验                             | 引擎边界          |
| 3   | worker contract map 按 R 申报（verify_contract_map 扩展）                                           | M3 评审链         |
| 4   | 引擎 join 出 ledger + invariant 硬检查 + NEEDS_RULING 终态语义（fail-closed）                       | workflow 终态路径 |
| 5   | P-080①②：compact 默认返回 + >2KB 指针化 + flag 位规范（= §6 Tier 0）                                | MCP 返回层        |
| 6   | Tier 1 规则化清场（AgentMesh 侧会话，workflow 终态时占位符归档）                                    | 同 4 的时点       |

**Batch 2 —— 副官与分诊（最高风险批次：config flag 灰度，副官失败回退组长手写）**

| #   | 改动                                                                                                                          |
| --- | ----------------------------------------------------------------------------------------------------------------------------- |
| 7   | 副官工具 `distill_requirements` / `summarize_for_leader`（免费模型；输出按 §7 双路 schema 强制；探针先行 + fail-closed 回退） |
| 8   | 分诊引擎规则 + gated 闸道边缘裁决（run 前同步裁决——组长刚出完 spec 仍在环内，不依赖终态推送）                                 |
| 9   | 快车道抽样器（seeded 随机 10-20%，可审计）+ 越界升道（tree guard 对照 `acceptance.files`）                                    |
| 9.5 | 延迟感知路由：候选链排序加 p50 时延维度（**自 Batch 3 提前**——快车道中位数由它决定，是速度目标关键路径）                      |

**Batch 3 —— 推送与兜底**

| #   | 改动                                                                                                                                                      |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 12  | P-080⑤：终态 MCP notifications 推送替代轮询（此前靠 event-driven poll_task 收 compact 契约，返回体已截断，轮询成本可控；推送是体验优化而非省 token 关键） |
| 13  | Tier 2 LLM 压缩兜底（阈值触发 compact_context，比例可配；作用域 = AgentMesh 会话）                                                                        |
| 14  | 抽检缺陷人工回流分诊判据的流程固化（修订记录进 PROBLEMS.md）                                                                                              |

## 10. A/B 验证口径（用现有 stats 基建）

| 指标                       | 基线     | 目标                                                              | 采集                                              |
| -------------------------- | -------- | ----------------------------------------------------------------- | ------------------------------------------------- |
| leaderShare（组长/总消耗） | ≈75-80%  | Batch 1 后 ≤40-50%（组长仍手写条目）；Batch 2 后 ≤15%             | stats 按角色聚合（已有）+ lane 聚合（Batch 1 #0） |
| 需求可对账率               | 不可证   | 100%（invariant 硬保证——**不丢账**，非语义正确）                  | ledger invariant 字段                             |
| 语义正确性                 | 不可证   | 快车道缺陷逃逸率 <5%；标准/完整道沿用评审既有口径                 | 抽检发现缺陷数 / 抽检总数                         |
| 简单任务墙钟（中位数）     | 20-25min | 5-12min（前置：延迟路由 + P-079；未落地时只报中位数，尾部不承诺） | metrics.jsonl（已有）                             |
| 蒸馏失真                   | —        | 引用核验拦截率 + 组长抽查通过率（出处≠忠实，忠实度单独量）        | 蒸馏核验日志                                      |

验证方式：下一个项目快车道/标准任务自然混跑形成对照，无需专门构造。

## 11. 风险与对策

| 风险                                  | 对策                                                                       | 残余                                  |
| ------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------- |
| 蒸馏语义合并（2 条并 1 条）           | 引用 substring 核验（只保出处）+ 对数粗核验 + 组长裁决抽查                 | 中（v2 上调：substring 不保改写忠实） |
| 快车道越界（worker 写声明集外文件）   | tree guard 检出即升道补评审（§5 升道规则）                                 | 低                                    |
| 副官档位不稳（免费档秒败/僵死前科）   | 探针先行 + fail-closed 回退组长手写                                        | 低                                    |
| 组长侧历史已膨胀（Tier 0 只防新注入） | 宿主侧纪律：新开会话 + contextSessionIds 续接；组长引擎内化 = vNext 非目标 | 使用纪律                              |
| 分诊误判（复杂任务进快车道）          | 10-20% 抽检 + 抽检缺陷人工回流规则                                         | 中                                    |
| compact 吞异常                        | flag 位强制（unresolvedP0P1/coverage/anomalies）                           | 低                                    |
| ledger join bug                       | invariant 硬检查失败即告警（fail-closed）                                  | 低                                    |
| opencode 无原生 schema 强制           | Zod 校验 + 1 次重试 + fail-closed                                          | 低                                    |
| EARS 表达不了主观需求（"美观"）       | decidable:false → PENDING_RULING 人审区                                    | 设计内                                |
| 编码任务并行度低（Anthropic 警告）    | 文档/生成类任务优先并行；纯编码谨慎评估                                    | 使用纪律                              |
| 副官是新单点                          | 副官输出也走引用核验 + schema 强制                                         | 低                                    |

## 12. 与 PROBLEMS 立案衔接

- **P-080**：①②落 Batch 1、⑤落 Batch 3、③即副官工具（Batch 2）、④（leaderShare 指标+告警）v2 提前至 Batch 1 #0——立案完整解
- **P-079**：正交可靠性主线（TRANSIENT 有界退避重试 + `workflow run --resume`），独立推进不混批；错误分类参照 codex is_retryable 划分
- **P-075~078**：已修复项不受影响

## 13. 诚实边界

1. 状态机保证流程确定，不保证内容正确——"恰好骗过验收+评审+抽检"的缺陷无法根除，最终兜底是 PENDING_RULING/escalated 人工裁决
2. EARS 不是万能语法：非行为类需求（视觉、口味）只能进人审区，占比高时对账单省的 token 会被裁决轮吃回去
3. 目标数字分层：≤15% 是 Batch 2 后目标（Batch 1 只承诺 ≤40-50%）；5-12min 是中位数目标且前置延迟路由 + P-079——以下一个项目 A/B 实测为准；`stats` 已有基建，验证成本低
4. 副官蒸馏链路新增了一层失真可能——引用核验+对数检查把已知失真模式堵住，未知模式靠组长抽查与抽检回流发现
5. Tier 0 只防"新的大输出进入"，**不能回收组长会话里已存在的历史**——宿主侧上下文引擎不可改写，这是 v0.5 的硬边界；组长内化为引擎 agent（彻底解法）是 vNext 非目标
6. quote substring 核验只证明出处、不证明 EARS 改写忠实；中文散文需求多句合成时 substring 取不出，此类条目会大量落 decidable:false 人审区——实际人审占比待首个项目实测校准，占比过高时省下的 token 会被裁决轮吃回（与 2 同源）
