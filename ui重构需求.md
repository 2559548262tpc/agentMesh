好的，选第 2 个方向更对：浅色 SaaS 风更适合真实后台产品，信息层级也更稳。  
我把前面的 UI 需求和《AgentMesh 可视化面板 UI 迭代前端需求文档》合并成一版可直接交付的前端需求。

---

# AgentMesh 可视化面板 UI 迭代前端需求文档

## 1. 需求背景

当前 AgentMesh 可视化面板需要从原来的「按会话 / bridge-sess 拆分展示」重构为「按任务聚合展示」。  
目标是让用户以 **任务** 为核心单位查看、切换和管理 AI Agent 的执行过程，包括任务指令、执行组员、检查结论、Token 消耗、实时终端日志等信息。

---

## 2. 需求目标

1. 将同一任务下的多个 Worker 聚合到同一条任务条目中。
2. 左侧侧边栏展示任务列表，而不是展示会话列表或 bridge-sess ID。
3. 中间区域展示当前选中任务的详情、任务指令、任务产出和实时终端。
4. 右侧区域展示当前任务的检查结论与 Token 消耗统计。
5. 终端日志与任务强绑定，只在选中任务且任务执行时展示。
6. 整体 UI 升级为浅色、专业、SaaS 化的后台管理面板风格。

---

## 3. 设计风格定义

### 3.1 整体风格

- **产品类型**：桌面端 AI Agent 管理后台
- **主色调**：蓝绿色 / 科技蓝作为主操作色
- **背景色**：浅灰或白色背景，降低长时间使用疲劳感
- **卡片风格**：圆角卡片 + 轻阴影 + 细分割线
- **信息风格**：清晰、克制、工程化，避免过度炫技
- **字体层级**：标题加粗、正文常规、辅助信息小字弱化
- **状态表达**：进行中、已通过、失败、待检查需明确区分

### 3.2 推荐色彩规范

| 类型       | 颜色建议                 |
| ---------- | ------------------------ |
| 主色       | #165DFF / #00B42A 蓝绿系 |
| 背景       | #F7F8FA                  |
| 卡片       | #FFFFFF                  |
| 标题       | #1D2129                  |
| 正文       | #4E5969                  |
| 辅助文字   | #86909C                  |
| 成功状态   | #00B42A                  |
| 警告状态   | #FF7D00                  |
| 错误状态   | #F53F3F                  |
| 进行中状态 | #165DFF                  |

---

## 4. 整体布局结构

整体保持三栏布局：

| 区域             |      宽度建议 | 说明                               |
| ---------------- | ------------: | ---------------------------------- |
| 左侧任务侧边栏   | 260px - 320px | 展示任务列表、搜索、任务状态       |
| 中间任务详情区   |     50% - 60% | 展示任务指令、任务产出、实时终端   |
| 右侧检查与统计区 | 320px - 360px | 展示检查结论、Token 消耗、组员信息 |

---

## 5. 左侧任务侧边栏设计

### 5.1 顶部区域

左侧侧边栏顶部应包含：

- 产品名称 / Logo：`AgentMesh`
- 搜索输入框：文字为「搜索任务名称」
- 可选筛选器：如「全部任务」「进行中」「已通过」「失败」

### 5.2 任务列表展示

任务列表不再按 `bridge-sess` 或会话维度展示，而是按 `task` 维度聚合展示。

每条任务条目展示：

| 信息       | 说明                       |
| ---------- | -------------------------- |
| 任务标题   | 提取自任务指令中的任务名称 |
| 任务状态   | 进行中 / 已通过 / 失败     |
| 使用模型   | 当前任务选用的模型名称     |
| Token 消耗 | 本任务累计 Token           |
| 任务摘要   | 可选，展示一句简短任务描述 |

示例：

```text
可视化面板 UI 重构
Ling 3.0 Flash · Token 12,480 · 进行中
```

---

### 5.3 任务状态规则

| 状态   | 展示条件             | 视觉样式   |
| ------ | -------------------- | ---------- |
| 进行中 | 任务未完成且正在执行 | 蓝色状态点 |
| 已通过 | 任务完成且检查通过   | 绿色状态点 |
| 失败   | 任务失败或检查未通过 | 红色状态点 |
| 待检查 | 任务完成但未检查     | 橙色状态点 |

---

## 6. 中间任务详情区设计

中间区域与左侧选中任务强绑定。

### 6.1 顶部标题区

标题区展示：

- 任务标题
- 任务状态标签
- 任务模型标签
- 操作按钮：如「重新执行」「查看日志」「复制结果」

示例：

```text
可视化面板 UI 重构
Ling 3.0 Flash · 进行中

[重新执行] [复制结果]
```

---

### 6.2 任务指令卡片

展示组长派发的完整任务指令。

卡片标题：

```text
任务指令
```

内容支持 Markdown 展示，包括：

- 任务背景
- 任务目标
- 涉及文件
- 底稿路径
- 检查要求
- 其他原始指令信息

---

### 6.3 任务产出卡片

展示 Worker 完成任务后的输出结果。

卡片标题：

```text
任务产出
```

内容包括：

- 执行总结
- 改动清单
- 文件变更
- 代码片段
- 验证结论
- 失败原因（如失败）

---

### 6.4 实时终端窗口

中间区域底部增加任务绑定的实时终端。

终端标题：

```text
实时终端
```

终端展示规则：

1. 只展示当前选中任务的终端日志。
2. 未选中任务时，不加载终端流。
3. 任务未执行时，展示占位文字：

```text
任务未执行，暂无实时终端输出
```

4. 任务执行时，展示实时输出：

```text
worker@agentmesh:~$ npm run dev
Terminal stream connected
Building task context...
Compiling UI components...
Task execution in progress...
```

5. 切换任务后，终端内容同步切换为对应任务日志。
6. 终端不需要持久化全部历史，只展示执行期间的实时流。

---

## 7. 右侧检查结论与统计区设计

右侧区域也必须跟随当前选中任务，不再展示全量会话混杂数据。

---

### 7.1 检查结论卡片

卡片标题：

```text
检查结论
```

展示当前任务的最近检查结果。

内容包括：

- 检查状态
- 检查时间
- 检查结论
- 通过 / 不通过项
- 问题清单
- 整改建议

示例：

```text
检查结论
最后检查：2026-06-02 14:30

✅ 代码结构清晰
✅ 接口字段完整
⚠️ 样式回归待验证
❌ 终端日志未完整绑定
```

---

### 7.2 任务组员卡片

卡片标题：

```text
任务组员
```

展示当前任务下的参与 Worker。

分为两类：

| 角色     | 说明                  |
| -------- | --------------------- |
| 干活组员 | 负责执行任务的 Worker |
| 检查组员 | 负责检查结果的 Worker |

展示字段：

- 组员名称
- 角色标签
- 使用模型
- 执行状态
- 完成时间

---

### 7.3 Token 消耗统计卡片

卡片标题：

```text
Token 消耗
```

展示当前任务的 Token 消耗。

建议展示：

| 字段         | 说明               |
| ------------ | ------------------ |
| 本任务总消耗 | 当前任务累计 Token |
| 组长消耗     | 组长 Agent 消耗    |
| 组员消耗     | 执行 Worker 消耗   |
| 检查消耗     | 检查 Worker 消耗   |

视觉上可使用环形图或横向进度条展示。

示例：

```text
总消耗 12,480
组长 3,200
组员 9,280
检查 1,800
```

---

## 8. 交互逻辑设计

### 8.1 任务切换逻辑

用户点击左侧任务条目后：

1. 左侧任务条目变为高亮选中状态。
2. 中间区域加载该任务的指令、产出和终端。
3. 右侧区域加载该任务的检查结论、组员信息和 Token 统计。
4. 终端只拉取当前任务的实时日志流。

---

### 8.2 终端展示逻辑

| 场景           | 表现                                               |
| -------------- | -------------------------------------------------- |
| 未选中任务     | 终端区域不展示日志，提示「请选择一个任务查看终端」 |
| 选中未执行任务 | 展示「任务未执行，暂无实时终端输出」               |
| 选中执行中任务 | 展示实时终端流                                     |
| 切换到其他任务 | 终端内容切换，不继续展示上一任务日志               |
| 任务结束       | 展示最终执行结果，不主动无限拉取历史               |

---

### 8.3 任务聚合逻辑

一个任务包含：

```text
Task
├─ 任务标题
├─ 任务状态
├─ 任务指令
├─ 任务产出
├─ 使用模型
├─ 总 Token 消耗
├─ 干活组员
├─ 检查组员
└─ 实时终端日志
```

前端不再以 `bridge-sess` 或单条会话作为最小展示单元，改为按 `task` 聚合。

---

## 9. 页面组件建议拆分

前端可拆分为以下主要组件：

| 组件                   | 说明               |
| ---------------------- | ------------------ |
| `TaskSidebar`          | 左侧任务侧边栏     |
| `TaskSidebarItem`      | 单条任务条目       |
| `TaskDetailHeader`     | 任务详情头部       |
| `TaskInstructionCard`  | 任务指令卡片       |
| `TaskOutputCard`       | 任务产出卡片       |
| `TaskTerminal`         | 任务绑定实时终端   |
| `CheckConclusionCard`  | 检查结论卡片       |
| `TaskWorkerList`       | 任务组员列表       |
| `TokenConsumptionCard` | Token 消耗统计卡片 |

---

## 10. 核心页面状态

### 10.1 默认状态

未选择任务时：

- 中间区域展示空状态：「请选择左侧任务查看详情」
- 右侧区域展示：「暂无任务数据」
- 终端不加载日志

---

### 10.2 任务执行中状态

选中正在执行的任务时：

- 中间终端区域展示实时日志
- 任务状态显示「进行中」
- 右侧 Token 消耗持续更新
- 检查结论区域显示「检查进行中」或「等待执行完成」

---

### 10.3 任务完成状态

任务完成后：

- 任务状态变为「已通过」或「失败」
- 任务产出展示最终结果
- 检查结论展示最终检查结果
- 终端保留最后输出，但停止实时拉取

---

## 11. UI 优化重点

### 11.1 信息层级优化

当前面板容易让用户混淆「任务、会话、Worker、终端」。  
新版需要强化以下层级：

```text
任务级信息
├─ 任务标题
├─ 任务状态
├─ 任务指令
├─ 任务产出
├─ 检查结论
├─ Token 统计
└─ 实时终端
```

---

### 11.2 视觉优先级

建议视觉优先级：

1. 任务标题
2. 任务状态
3. 任务指令
4. 任务产出
5. 实时终端
6. 检查结论
7. Token 消耗

---

### 11.3 卡片密度

建议采用中等密度：

- 卡片内边距：`16px - 24px`
- 模块间距：`16px - 24px`
- 标题与正文间距：`8px - 12px`
- 小字信息降低一级透明度，避免干扰主内容

---

## 12. 非需求范围

本次不涉及：

1. 不改变底层 Agent 调度逻辑。
2. 不改变后端核心任务执行逻辑。
3. 不做终端日志持久化存储。
4. 不做复杂权限系统改造。
5. 不做全局聊天会话界面重构。

本次重点是：

> 前端展示层按任务聚合，重构任务、Worker、检查结论、终端和 Token 消耗的展示关系。

---

## 13. 最小可交付版本

MVP 版本建议交付以下能力：

| 模块       | 能力                   |
| ---------- | ---------------------- |
| 任务列表   | 按任务聚合展示         |
| 任务切换   | 点击任务切换详情       |
| 任务指令   | 展示 Markdown 任务说明 |
| 任务产出   | 展示执行结果           |
| 实时终端   | 与选中任务绑定         |
| 检查结论   | 展示当前任务检查结果   |
| Token 统计 | 展示当前任务消耗       |
| 任务状态   | 进行中 / 已通过 / 失败 |

---

## 14. 一句话需求

将 AgentMesh 可视化面板从「按会话展示」重构为「按任务聚合展示」，左侧展示任务列表，中间展示任务指令、任务产出和任务绑定实时终端，右侧展示当前任务的检查结论与 Token 消耗统计，整体升级为浅色、专业、SaaS 化的后台管理界面。
