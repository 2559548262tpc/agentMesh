# ORCHESTRATION.BASE.md — 编排通用契约范本（跨项目复用）

> **用法**：本文是稳定契约的沉淀层，跨项目长期复用，只在三种时机读取——① 新项目初始化时（复制第 3 节模板）；② 裁决争议/返工依据时；③ 复盘修订契约时。**每轮开工只读项目宪法（ORCHESTRATION.md），不要默认通读本文。**
> 基础纪律以 `AGENTS.md` 的 ORCHESTRATOR DISCIPLINE 为准，本文不重复，只沉淀实测教训与效率规则。

## 1. 通用编排纪律（r14-r21 实测沉淀，缺一不派）

### 1.1 派发检查单

1. **background:true** — 所有长任务一律后台派发；同步只留给 `get_session`/`list_agents` 快查询。
2. **contextSessionIds** — 交接必传上游 Bridge 会话 id，否则面板判孤儿、归属失真。
3. **角色显式确认** — worker/reviewer/tester 逐一过目；需独立验证的任务必须显式派 tester。
4. **简报自足** — 文件路径、具体改动、验收标准齐备；禁止 "based on your findings" 式转引。
5. **并行切分** — 契约先行后按文件集拆包并行；包粒度写进派发记录。
6. **数据根一致** — 派发前确认 bridge/面板/CLI 读同一数据目录；注意数据根可能由 env 指向非默认路径（r21 实测 `F:\agentmesh-data` ≠ `~/.agentmesh`）。
7. **角色配置实存** — 派发前检查目标 cwd 下 `.agentmesh/config.json` 实际存在且角色绑定正确（r21-3 教训：声称建好 ≠ 实存）。
8. **复杂度闸门** — 单文件高内聚 → 组长直接改；多文件可拆并行 → MCP 派发；"不值得派发"是合法结论。

### 1.2 止损与换道（失败要尽早暴露，不要等）

- **免费模型止损阈值**：后台任务输出 0 字节超过 2 分钟 → 判定 vendor 停滞，立即弃置换道（r21：nemotron 两次 0 字节 5-6 分钟全废弃；big-pickle 流式正常可作对照基准）。
- **换道顺序**：沿 candidates 链换模型 → 换通道（claude TLS 失败无额度时弃用）→ 上报人工。
- ** vendor 截断识别**：`exitCode=0` 但 `status=failed` 且 finalAnswer 是中途叙述 = 会话被过早截断，按 vendor 失败换道重派，不要重试同参数。
- **失败分级**：桥接层错误看 error_code 自动重试；模型拒绝沿升级链重派；返工闭环 3 轮仍 FAIL 带证据链上报。

### 1.3 评审与测试流水线（两段式 + 并行）

1. **两段式评审**：先做「契约映射快审」（每条契约 → 对应代码位置，缺一条即 FAIL，1 分钟级），通过后再做深度评审。方向性失败在快审终止，不再烧深度评审。
2. **评审禁令前置声明**：评审简报必须显式写明"禁止执行命令"（opencode reviewer 走 plan 模式，执行命令会话即夭折，P-R21-1）；运行验证归 tester。
3. **tester 与 reviewer 并行**：两者都是只读，tester 的运行验证不依赖评审结论，可同时派发；返工后 tester 只增量复测受影响条目。
4. **复审只看增量**：复审轮默认只审上轮返工改动的文件，不做全量冷启动重读。
5. **裁决标注**：最终报告必须以 PASS/FAIL 开头；vendor 截断（无裁决文本）一律 UNKNOWN，不得与 FAIL 混淆。

### 1.4 可执行 SPEC（验收前移，治方向性返工）

- SPEC 写成「机器可核对契约」的同时，**必须附带官方验收测试文件**（如 `acceptance.test.js`，worker 不可修改），worker 交付前必须跑通。
- 效果：方向性错误在 worker 内部数分钟内暴露，而非整个实现完成后才被评审发现；tester 复跑同一文件即可，评审只审实现质量与作弊。
- 配套：SPEC 中每个行为契约给出可执行的输入/输出样例，禁止"好用""美观"类不可判定词。

### 1.5 安全姿态

- prompt-only 通道假设会照做任何指令：任务文本不含破坏性命令示例；危险操作改派 enforced 沙箱通道或人工执行。
- worker 声称"测试通过"必须查 `testFilesModified` 证据；评审结论 PASS 前必须澄清 SECURITY/SAFETY 警告。
- worker 行为越界（删文件、改无关模块）→ `rollback_task` 回滚重派。

### 1.6 验收与证据

- **done 的定义**：实现类任务只有"测试实际运行通过 + 变更摘要"算完成；`git diff` + 测试输出才算数，自述不算。
- **诚实失败优于粉饰成功**：失败如实记录原因码与证据链后上报。
- **不采集的指标不虚报**：资源监控未采样的 CPU/RSS 明确写"未采集"，不填零。

## 2. 产品侧待办（AgentMesh 本体改进，编排纪律解决不了的）

> 详细证据链见 `real_test.md` 第二十一轮 P-R21-1~7。

1.（P0）评审适配层：opencode plan 模式夭折恢复（无裁决结束记 UNKNOWN 并支持续跑）；评审/返工事件流接入 output 文件；stalled 判定纳入子进程活动信号。2.（P1）`cancel_task(taskId)`：杀进程树 + 落盘 cancelled 终态；stalled 自动提示 `hint.nextCandidates`。3.（P1）面板终态语义：worker 子任务状态收敛（消费最后 mcpCall + terminal.bgStatus）；verdict 推导加末尾匹配；执行失败与裁决 FAIL 分离。4.（P2）树守卫默认排除 `.agentmesh/**`。5.（P1）`delegate_task` 模型 ID 校验失败返回候选列表；`review_changes` `excludePaths` 参数（r19 §11 遗留，P-R19-1/3/4）。

## 3. 新项目宪法模板（复制到 ORCHESTRATION.md 后填写）

```markdown
# ORCHESTRATION.md — {项目名}项目宪法

> 通用约束见 ORCHESTRATION.BASE.md（裁决争议时才读）。本文只写本项目特有内容，开工先读本文。

## 1. 目标

{一句话业务目标 + 需求源文件指针}

## 2. 验收标准（可判定）

{测试退出码 / 文件存在 / 命令输出断言；禁止主观词。逐条编号，返工以此为据}

## 3. 项目特有契约（语义定稿，组员不得更改）

{接口形状 / 数据流 / 分工边界等本项目专属决定}

## 4. 非范围

{明确不做什么}

## 5. 分工与模型

| 角色     | 通道 | 模型 |
| -------- | ---- | ---- |
| 组长     | —    | —    |
| worker   |      |      |
| reviewer |      |      |
| tester   |      |      |

## 6. 交付物

{交付文件清单 + 验收测试文件名（可执行 SPEC）}
```

## 4. 修订规则

- 项目中途改需求：先改 ORCHESTRATION.md，再重派受影响任务——不算失败，不追溯。
- 每轮复盘产出的通用教训 → 沉淀进本文对应小节；项目特有教训 → 留在该项目宪法或 real_test.md。
- 本文与 AGENTS.md 冲突时以 AGENTS.md 为准；与项目宪法冲突时以项目宪法为准（项目宪法更具体）。
